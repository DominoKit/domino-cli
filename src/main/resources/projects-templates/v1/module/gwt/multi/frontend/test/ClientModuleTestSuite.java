package ${rootPackage}.${subpackage}.client;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses(${prefix}ProxyTest.class)
public class ${moduleName}ClientModuleTestSuite {
}
